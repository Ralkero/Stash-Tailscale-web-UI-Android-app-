# Stash Tailscale Web UI Android App

Private Android WebView wrapper for accessing a Stash server over Tailscale.

This repository is being populated from the local Android project at `android-stash-wrapper`.
